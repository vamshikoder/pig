//~ This file is used to post the data to the server or to the local storage.
//& Made by PIG

class PostDataService {
  //*to local storage
  void toLocal() {
    ///[toLocal] should be the prefix of all the functions written ex[toLocalGetNotifications]
  }
  //*to server
  Future toServer() async {
    ///[toServer] should be the prefix of all the functions written ex [toServerGetNotifications]
  }
}
