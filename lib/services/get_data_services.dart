//~ This file is used to get the data from the server or the local storage.
//& Made by PIG
class GetDataService {
  //*from local storage
  void fromLocal() {
    ///[fromLocal] should be the prefix of all the functions written ex[fromLocalGetNotifications]
  }
  //*from server
  Future fromServer() async {
    ///[fromServer] should be the prefix of all the functions written ex [fromServerGetNotifications]
  }
}
