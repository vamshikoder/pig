import 'package:flutter/material.dart';

class Dummiw extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: const Text('hello'),
    );
  }
}
