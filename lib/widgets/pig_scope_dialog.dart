//^ Available only to authorized users
//~ This dialog is used to set the scope of events, notifications, notes.
//& Made by PIG

import 'package:flutter/material.dart';
// import 'package:get/route_manager.dart';

// import '../config/config.dart';

// import '../widgets/global_utility_widgets.dart';

Future buildPigScopeDialog(BuildContext context) {
  return showDialog(
      context: context,
      builder: (context) {
        return Container();
      });
}
