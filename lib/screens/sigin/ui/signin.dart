import 'package:flutter/material.dart';

import '../../../config/config.dart';

class Signin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: heading('sigin screen'),
      ),
    );
  }
}
