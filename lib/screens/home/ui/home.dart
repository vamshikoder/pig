import 'package:flutter/material.dart';
import 'package:pig/config/config.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: heading('home screen'),
      ),
    );
  }
}
