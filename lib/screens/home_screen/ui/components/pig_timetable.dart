//~ A custom TimeTable
//TODO need to implement
//& Made by PIG

import 'package:flutter/material.dart';
import '../../../../config/config.dart';

class PigTimeTable extends StatelessWidget {
  const PigTimeTable({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SubText('hello from title2');
  }
}
